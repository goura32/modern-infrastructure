# Copy to .env for a manual Compose run. Do not commit .env.
POSTGRES_DB=training
POSTGRES_USER=training
POSTGRES_PASSWORD=replace-with-a-local-secret
DB_HOST=db
DB_PORT=5432
DB_NAME=training
DB_USER=training
DB_PASSWORD=replace-with-the-same-local-secret
